@using Microsoft.AspNet.Identity
@if (User.IsInRole("Admin"))
{
    
        <li>@Html.ActionLink("User Management", "SeeAllUsers", "Manage")</li>
        <li>@Html.ActionLink("Session Management", "Index", "SessionType")</li>
    
    
}
else
{
    
}